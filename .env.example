DATABASE_URL=postgresql://user:password@localhost:5432/antibody_repo
JWT_SECRET=change-this-to-a-random-string
PORT=3000
NODE_ENV=development

# For cloud databases (Neon, Render), use the full connection string:
# DATABASE_URL=postgresql://user:password@ep-xyz.eu-central-1.aws.neon.tech/antibody_repo?sslmode=require

# Optional: restrict CORS to a specific origin in production
# CORS_ORIGIN=https://antibody-repository.onrender.com
